CREATE TABLE scripts (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id text NOT NULL, title text NOT NULL, category text NOT NULL, body text NOT NULL, created_at timestamptz NOT NULL DEFAULT now());